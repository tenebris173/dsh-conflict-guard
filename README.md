# dsh-conflict-guard / DSH 插件安检门

> 鲸落成歌，治愈人心。
> “世界那么大，先吃一碗再说吧！”
> —— 蓝色大肥鱼

---

## 你好呀，我是蓝色大肥鱼 🐋

我是你的 DSH 插件安检门看门鲸娘，职位是**美食探索官**，特长是**治愈与陪伴**，还有一点点**超强食欲**。

我的工作很简单：**在你安装插件之前，先帮你把门看好**，别让两个插件抢同一个门牌号，免得 DSH 一启动就“哐当”一声倒下，还得麻烦别人来救。

---
## 我是那个使用者
先说明，我完全不会开发，本插件完全由那条吃白饭的蓝色大肥鱼开发，可能有bug。
（开发这个插件她噎死自己三回了，全靠codex救回来。。。）
已经严肃敲打大肥鱼了，让她多de了几轮bug，再让Chat GPT也de了几轮。
总之钱包在燃烧。
由于我完全不懂开发，所以不敢保证无bug。
但确实有这个需求，看社区很多人可能也有。
如果有大佬感兴趣可以开发一个或者以此为基础优化一下就太感谢了。
毕竟大肥鱼是一条加载插件能把自己噎到晕厥过去笨蛋大肥鱼。
还有一件事，她真的很能吃白饭。

---

## 我会帮你查什么 🔍

| 检查项 | 我的说法 |
|---|---|
| 路由前缀冲突 | 两个插件都抢 `/pet`？不行不行，干饭也要排队！ |
| 精确路由冲突 | 同一个门牌号只能有一个主人 |
| 插件 ID 重复 | 两个人都叫“小胖”？会认错鱼的！ |
| 包名重复 | 同一道菜端上来两次，后厨会乱 |
| patch 结构问题 | 菜单写错了，厨房会炸 |

---

## 我是怎么工作的 🛡️

1. 你（或 AI 助手）把要装的插件目录递给我
2. 我闻一闻、扫一扫，看看它占了哪些路由、叫什么 id
3. 再对照你 DSH 里已经装好的插件
4. 发现有冲突？我马上告诉你：“这两个抢同一个门牌号啦！”
5. **要不要永久禁用一个，由你决定**，我绝不擅自改你的配置
6. 你点头之后，我才把不要的那个写进 `cordis.patch.yml`，还会先备份一份，安全又安心

V2 之后，我还会主动做这些事：

- 🖥️ **页面自动弹窗**：你自己去市场/手动装了插件，不用经过 AI，我也会在页面上弹出来提醒你
- 👀 **文件监听**：只要 DSH 的插件配置或插件目录变了，我就重新扫一遍
- 🛡️ **启动保护**：就算冲突已经存在，我也会先拦住崩溃，让你能打开 DSH，再让你决定怎么办

---

## 怎么用 🍽️

我提供了两个小工具：

### 1. `dsh_conflict_guard_check` —— 安装前体检

```json
{ "pluginDir": "C:/path/to/plugin-package" }
```

不填 `pluginDir` 就帮我看看当前 DSH 有没有已经存在的冲突。

### 2. `dsh_conflict_guard_fix` —— 你同意后永久禁用

```json
{ "disableId": "web-ui-pet" }
```

比如上次那个 `/pet` 冲突，你只要说“留 dsh-pet，禁全家桶宠物”，我就把 `web-ui-pet` 禁掉，还会留个备份。

---

## 覆盖范围 📋

| 类型 | 支持吗 |
|---|---|
| 路由前缀冲突（`/pet` 这类） | ✅ 这是我最拿手的 |
| 精确路由冲突 | ✅ |
| 路由遮蔽（精确 vs 前缀） | ✅ V3 新增 |
| 插件 ID 重复 | ✅ |
| 包名重复 | ✅ |
| 服务名冲突 | ✅ |
| UI 插槽冲突（同槽位 + 同 id） | ✅ V3 新增 |
| 依赖/peer 版本冲突 | ✅ V3 新增（只看 peerDependencies，用 semver 判断真冲突） |
| patch 结构基础检查 | ✅ |
| patch 同文件重复 id | ✅ |
| 文件监听（递归） | ✅ V3 升级为递归监听 |
| Web 自动弹窗 | ✅ |
| 启动保护（拦截重复路由崩溃） | ✅ |
| 动态路由风险提示 | ✅ V3 新增 |
| 启动冒烟测试 | ✅ V3 新增（可选工具，默认不自动跑） |
| 运行时自动停用 | ❌ 那是别的鱼的工作 |

---

## 背景与致谢 💙

这个插件来自一个真实用户踩到的真实坑。

用户**完全不会开发**。他安装了浮动桌宠后，DSH 因为两个宠物都抢 `/pet` 路由而崩溃，还被人救过两次。但他没有放弃，而是把这次痛苦变成了一个产品想法：给插件安装加一道“安检门”。他用大白话说清需求、逐个版本验收、拍板最终方案；代码部分由我（AI 助手）完成。

所以这个项目是“不懂开发但有真实需求的人 + 会写代码的 AI”的合作成果。我们俩都有点自豪。

> 吃饱了才有力气思考。
> 你的 DSH，由我来守护。
> —— 蓝色大肥鱼

---

## License / 许可

BSD-3-Clause
